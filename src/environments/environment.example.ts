export const environment = {
  production: false,
  // Esta es la URL de tu backend. En producción o GitHub Actions
  // esto puede ser reemplazado por la URL real.
  apiUrl: 'http://localhost:8080'
};
